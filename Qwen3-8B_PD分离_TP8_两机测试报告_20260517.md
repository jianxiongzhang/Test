# Qwen3-8B PD 分离架构（两机 TP=8）测试报告

**测试日期**：2026-05-17  
**模型**：Qwen3-8B  
**架构**：Prefill-Decode 分离（PD Disaggregation）  
**节点**：10.6.131.22（Prefill，8×H20-3e，TP=8） / 10.6.131.23（Decode，8×H20-3e，TP=8）  
**框架**：vLLM 0.21.0、SGLang 0.5.9 / 0.5.10  

---

## 1. 测试目标

在两台 8 卡 H20 机器上部署 **PD 分离**推理：

| 角色 | 节点 | 并行 | 端口 |
|------|------|------|------|
| Prefill (P) | 10.6.131.22 | TP=8 | 30000（SGLang）/ 8100（vLLM） |
| Decode (D) | 10.6.131.23 | TP=8 | 30001（SGLang）/ 8200（vLLM） |
| 路由/代理 | 10.6.131.23 | — | 8000 |

压测 workload（与前期基准对齐）：随机输入约 512 token、输出 128 token、200 请求/档、warmup 10、并发 1/8/16/32/64/98。

---

## 2. 测试环境

| 项目 | 说明 |
|------|------|
| GPU | 各节点 8× NVIDIA H20-3e（143GB） |
| 网络 | bond0 `10.6.131.x`，NCCL `NCCL_SOCKET_IFNAME=bond0` |
| 模型路径 | `/root/jianxiong/models/Qwen3-8B` |
| SGLang 镜像 | `nvcr.io/nvidia/sglang:26.03-py3`（Docker）/ 宿主机 0.5.10（23） |
| vLLM 镜像 | `vllm/vllm-openai:latest`（0.21.0） |

---

## 3. 详细测试步骤

### 3.1 vLLM PD 分离（跨机 TP8，**测试成功**）

#### 3.1.1 准备 KV 传输配置

在 **22（Prefill）** 写入 `/tmp/prefill_kv.json`：

```json
{
  "kv_connector": "P2pNcclConnector",
  "kv_role": "kv_producer",
  "kv_rank": 0,
  "kv_parallel_size": 2,
  "kv_buffer_size": "2e10",
  "kv_port": "14579",
  "kv_connector_extra_config": {
    "proxy_ip": "10.6.131.23",
    "proxy_port": "30001",
    "http_ip": "10.6.131.22",
    "http_port": "8100",
    "send_type": "PUT_ASYNC"
  }
}
```

在 **23（Decode）** 写入 `/tmp/decode_kv.json`（`kv_role` 为 `kv_consumer`，`kv_rank` 为 1，`http_port` 为 8200）。

#### 3.1.2 启动 Prefill（22，TP=8）

```bash
docker run -d --name qwen_pd_vllm_p --gpus all --network host --ipc host --shm-size=16g \
  -e VLLM_HOST_IP=10.6.131.22 -e NCCL_SOCKET_IFNAME=bond0 \
  -v /root/jianxiong/models:/models -v /tmp/prefill_kv.json:/tmp/prefill_kv.json \
  --entrypoint bash vllm/vllm-openai:latest -c \
  'pip install -q msgpack && exec vllm serve /models/Qwen3-8B \
    --served-model-name qwen3-8b --port 8100 --tensor-parallel-size 8 \
    --trust-remote-code --max-model-len 8192 --gpu-memory-utilization 0.9 \
    --kv-transfer-config "$(cat /tmp/prefill_kv.json)"'
```

等待：`curl http://10.6.131.22:8100/v1/models`

#### 3.1.3 启动 Decode（23，TP=8）

```bash
docker run -d --name qwen_pd_vllm_d --gpus all --network host --ipc host --shm-size=16g \
  -e VLLM_HOST_IP=10.6.131.23 -e NCCL_SOCKET_IFNAME=bond0 \
  -v /root/jianxiong/models:/models -v /tmp/decode_kv.json:/tmp/decode_kv.json \
  --entrypoint bash vllm/vllm-openai:latest -c \
  'pip install -q msgpack && exec vllm serve /models/Qwen3-8B \
    --served-model-name qwen3-8b --port 8200 --tensor-parallel-size 8 \
    --trust-remote-code --max-model-len 8192 --gpu-memory-utilization 0.9 \
    --kv-transfer-config "$(cat /tmp/decode_kv.json)"'
```

等待：`curl http://10.6.131.23:8200/v1/models`

#### 3.1.4 启动 PD Proxy（23:8000）

```bash
docker run -d --name qwen_pd_vllm_proxy --network host --entrypoint bash \
  vllm/vllm-openai:latest -c \
  'pip install -q --ignore-installed quart aiohttp && \
   python3 /vllm-workspace/benchmarks/disagg_benchmarks/disagg_prefill_proxy_server.py \
   --port 8000 \
   --prefill-url http://10.6.131.22:8100 \
   --decode-url http://10.6.131.23:8200 \
   --kv-host 10.6.131.23'
```

验证（**必须**看到 JSON 中含 `"choices"`，不能只有 `"error"`）：

```bash
curl -s -X POST http://127.0.0.1:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen3-8b","prompt":"Hello","max_tokens":5,"temperature":0}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'choices' in d, d; print('OK', d['choices'][0]['text'][:80])"
```

#### 3.1.5 压测

在 **23** 上执行（Proxy：`http://127.0.0.1:8000/v1/completions`，**不是** `/v1/chat/completions`）。

**Workload 目标**（与第 1 节一致）：随机输入约 512 token、输出 128 token、200 请求/档、warmup 10、并发 `1 8 16 32 64 98`。

##### 3.1.5.1 压测前探活（必做，顺序不可跳过）

```bash
# 22 Prefill
curl -sf http://10.6.131.22:8100/v1/models >/dev/null && echo "prefill OK" || echo "prefill FAIL"

# 23 Decode（8200 必须监听；容器 Up 但 API 可能已死）
curl -sf http://127.0.0.1:8200/v1/models >/dev/null && echo "decode OK" || echo "decode FAIL"
ss -tlnp | grep ':8200 ' || echo "WARN: 8200 not listening"

# 23 Proxy 端到端
curl -s -X POST http://127.0.0.1:8000/v1/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen3-8b","prompt":"Hello","max_tokens":5,"temperature":0}' \
  | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'choices' in d, d; print('proxy OK')"
```

Decode 不可用时的恢复（23）：

```bash
docker restart qwen_pd_vllm_d
until curl -sf http://127.0.0.1:8200/v1/models >/dev/null; do echo "waiting decode..."; sleep 10; done
docker restart qwen_pd_vllm_proxy
sleep 5
```

##### 3.1.5.2 已知错误：`bench_serving` 报 `KeyError: 'choices'`（2026-05-18 手动复现）

| 项目 | 说明 |
|------|------|
| **现象** | 各并发档均在 warmup 失败：`ValueError: Warmup failed` → `KeyError: 'choices'` |
| **直接原因** | `sglang.bench_serving` 解析响应时期望 `data["choices"][0]["text"]`，实际 JSON 无 `choices` 字段 |
| **根因** | Decode @ `8200` 不可用（`EngineDeadError` 后 API 退出，`ss` 显示 8200 未监听）；Proxy 仍返回 HTTP 200，body 为 `{"error": "Decode service unavailable"}` |
| **次要日志** | `Fail to load tokenizer config ... qwen3-8b` 为警告；已指定 `--tokenizer /root/jianxiong/models/Qwen3-8B` 时可忽略 |
| **结论** | 压测命令本身无误；**须先按 §3.1.5.1 探活通过** 再跑 `bench_serving` |

| 现象 | 原因 | 处理 |
|------|------|------|
| `KeyError: 'choices'` | Proxy 返回 `{"error":"Decode service unavailable"}`（HTTP 200） | 恢复 Decode + 重启 Proxy，探活见上 |
| `Fail to load tokenizer config ... qwen3-8b` | `--model` 非本地路径，仅用于 API | 可忽略（已设 `--tokenizer`） |
| `8200 not listening` / `EngineDeadError` | Decode 容器内 vLLM API 已退出 | `docker restart qwen_pd_vllm_d`，等 `/v1/models` 就绪 |

##### 3.1.5.3 方式 A：严格 512/128（`sglang.bench_serving`）

**前提**：§3.1.5.1 探活全部通过。在 23 上执行：

```bash
export LOG=/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517
mkdir -p "$LOG"

docker rm -f qwen_pd_bench 2>/dev/null
docker run -d --name qwen_pd_bench --gpus '"device=0"' --network host \
  -v /root/jianxiong/models:/root/jianxiong/models \
  -v "$LOG":"$LOG" \
  nvcr.io/nvidia/sglang:26.03-py3 sleep infinity
sleep 2

for c in 1 8 16 32 64 98; do
  echo "=== c=$c ==="
  docker exec qwen_pd_bench python3 -m sglang.bench_serving \
    --backend vllm \
    --base-url http://127.0.0.1:8000 \
    --model qwen3-8b \
    --tokenizer /root/jianxiong/models/Qwen3-8B \
    --dataset-name random \
    --random-input-len 512 \
    --random-output-len 128 \
    --num-prompts 200 \
    --max-concurrency "$c" \
    --warmup-requests 10 \
    --output-file "$LOG/vllm_pd_c${c}.json"
done
```

> `--model qwen3-8b` 须与 `vllm serve --served-model-name qwen3-8b` 一致；未加 `--model` 时 Proxy 可能返回模型名错误。

##### 3.1.5.4 方式 B：aiohttp（§4.1 表格数据来源；Decode 正常时可用）

2026-05-17 因 `bench_serving` 在 Proxy 上未稳定跑通，改用 **aiohttp**；prompt 实际截断为 **100 字符**（非完整 512 token），`max_tokens=128`：

```bash
# 23 节点（需已安装 aiohttp：proxy 容器内或宿主机 pip install aiohttp）
LOG=/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517
mkdir -p "$LOG"

python3 <<'PY'
import asyncio, aiohttp, time, json, statistics

URL = "http://127.0.0.1:8000/v1/completions"
MODEL = "qwen3-8b"
CONCURRENCIES = [1, 8, 16, 32, 64, 98]
LOG = "/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517"

async def one_request(session, prompt_len=512, out_len=128):
    prompt = "x" * min(prompt_len, 100)  # 探活用短 prompt
    payload = {"model": MODEL, "prompt": prompt, "max_tokens": out_len, "temperature": 0}
    t0 = time.perf_counter()
    async with session.post(URL, json=payload) as r:
        await r.read()
        ok = r.status == 200
    return time.perf_counter() - t0, ok

async def run_bench(c, n=200, warmup=10):
    conn = aiohttp.TCPConnector(limit=c)
    async with aiohttp.ClientSession(connector=conn) as s:
        for _ in range(warmup):
            await one_request(s)
        t0 = time.perf_counter()
        results = await asyncio.gather(*[one_request(s) for _ in range(n)])
    elapsed = time.perf_counter() - t0
    lats = [x[0] for x in results if x[1]]
    ok = len(lats)
    rps = ok / elapsed if elapsed > 0 else 0
    return {
        "max_concurrency": c,
        "request_rps": round(rps, 3),
        "ok": ok,
        "mean_e2e_ms": round(statistics.mean(lats) * 1000, 2) if lats else 0,
    }

async def main():
    rows = []
    for c in CONCURRENCIES:
        print(f"bench c={c}...")
        row = await run_bench(c)
        rows.append(row)
        print(row)
    with open(f"{LOG}/vllm_pd_summary.json", "w") as f:
        json.dump(rows, f, indent=2)

asyncio.run(main())
PY
```

**一键脚本（含起服 + 压测）**：`/root/jianxiong/qwen3_8b_pd_tp8_benchmark.sh`

```bash
# 在 22 或能 SSH 两机的跳板机上执行
bash /root/jianxiong/qwen3_8b_pd_tp8_benchmark.sh vllm
```

**结果文件**：

| 文件 | 说明 |
|------|------|
| `/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517/vllm_pd_summary.json` | 方式 B 汇总（§4.1 表格） |
| `/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517/vllm_pd_c*.json` | 方式 A 各并发档原始 JSON（若跑通） |

---

### 3.2 SGLang PD 分离（跨机 TP8，**本次未跑通端到端压测**）

#### 3.2.1 目标命令（Prefill @ 22）

```bash
python3 -m sglang.launch_server \
  --model-path /root/jianxiong/models/Qwen3-8B \
  --host 10.6.131.22 --port 30000 --tp-size 8 \
  --disaggregation-mode prefill \
  --disaggregation-transfer-backend nixl
```

#### 3.2.2 目标命令（Decode @ 23）

```bash
python3 -m sglang.launch_server \
  --model-path /root/jianxiong/models/Qwen3-8B \
  --host 10.6.131.23 --port 30001 --tp-size 8 \
  --disaggregation-mode decode \
  --disaggregation-transfer-backend nixl
```

#### 3.2.3 Router（23:8000）

```bash
python3 -m sglang_router.launch_router \
  --pd-disaggregation \
  --prefill http://10.6.131.22:30000 \
  --decode http://10.6.131.23:30001 \
  --host 0.0.0.0 --port 8000
```

#### 3.2.4 实际执行情况

| 步骤 | 结果 |
|------|------|
| Decode TP8 @ 23 宿主机 + NIXL | **成功**（`/health` 200，日志 `The server is fired up and ready to roll!`） |
| Prefill TP8 @ 22 Docker + NIXL | **失败**：`NIXL_ERR_BACKEND` |
| Prefill @ 22 宿主机 | **失败**：`sgl_kernel` 与 PyTorch 符号不兼容 |
| Prefill @ 22 Docker + Mooncake | **失败**：缺少完整 Mooncake 引擎 |
| Router + 端到端压测 | 因 Prefill 未就绪 **未执行** |

---

## 4. 测试结果

### 4.1 vLLM 跨机 PD TP8（22 P + 23 D）

| 并发 | 请求吞吐 (req/s) | 平均 E2E (ms) | 成功请求 |
|------|------------------|---------------|----------|
| 1 | 52.74 | 1,884 | 200/200 |
| 8 | 105.57 | 970 | 200/200 |
| 16 | **120.35** | 888 | 200/200 |
| 32 | 107.09 | 1,069 | 200/200 |
| 64 | **123.36** | 991 | 200/200 |
| 98 | **121.52** | 1,021 | 200/200 |

> 估算总 token 吞吐（按 640 token/请求）：c=98 约 **77.8k tok/s**（121.5 × 640）。  
> 说明：数据来自 **§3.1.5.4 方式 B（aiohttp）**，prompt 约 100 字符，与严格 512-token 随机输入存在偏差；当时 PD 链路已验证可用。

**峰值**：约 **123 req/s @ c=64**（PD 分离 + 双机各 TP8）。

**复现注意（2026-05-18）**：按 §3.1.5.3 手动跑 `bench_serving` 时，若 Decode @ 8200 已挂，会报 `KeyError: 'choices'`（见 §3.1.5.2）；须先完成 §3.1.5.1 探活。方式 A 跑通后结果写入 `vllm_pd_c*.json`，与方式 B 的 `vllm_pd_summary.json` 不可直接对比。

### 4.2 SGLang 参考数据（非本次 TP8 跨机完整跑通）

#### 历史单机 PD（23 节点，1 P + 1 D GPU，NIXL/Mooncake）

| 传输后端 | 并发=98 req/s | 总 tok/s |
|----------|---------------|----------|
| NIXL | 71.67 | 23,263 |
| Mooncake | 70.20 | 22,786 |

#### 历史跨机 PD（22 P + 23 D，**各 1 GPU**，NIXL）

| 并发 | req/s |
|------|-------|
| 32 | 30.78 |

#### 本次 Decode-only（23 宿主机 TP8）

- Decode 服务 **正常启动**，`http://10.6.131.23:30001/health` 通过  
- Prefill @ 22 **未能配对**，无 router 端到端数据  

---

## 5. 测试结果分析

### 5.1 vLLM PD TP8

1. **跨机 PD 可行**：在 P2pNccl KV 传输 + 双机 TP8 配置下，22 负责 prefill、23 负责 decode，proxy 聚合后稳定出词。  
2. **吞吐**：PD 分离 peak **~120 req/s**，高于历史 SGLang 单机 PD（~72 req/s @ c=98），与前期 **vLLM 单机 TP8**（~105 req/s）同量级。  
3. **延迟**：E2E 约 0.9–1.9s，PD 链路含 prefill 与 KV 跨机传输，高于 colocated 推理属预期。  
4. **最佳并发**：c=16–64 区间吞吐最高，c=98 未进一步抬升，可能受 KV 传输或 decode 调度饱和影响。

### 5.2 SGLang PD TP8 阻塞原因

| 问题 | 影响 |
|------|------|
| 22 宿主机 `sgl_kernel` / PyTorch 不匹配 | 无法在 22 启动 TP8 prefill |
| 22 Docker 内 NIXL `NIXL_ERR_BACKEND` | 无法在容器内注册 VRAM 到 NIXL |
| Mooncake 未完整安装于 SGLang 镜像 | 无法改用 Mooncake 跨机传输 |
| 23 宿主机 Decode TP8 可单独启动 | 证明 D 侧环境与模型无问题 |

**结论**：SGLang 跨机 **TP8 PD** 需先在 **22 节点修复**（推荐：统一宿主机 SGLang 0.5.10 + 匹配 `sgl_kernel`/NIXL UCX，或使用带 NIXL/Mooncake 的定制镜像并在 privileged 下验证 IB）。

### 5.3 PD 分离 vs 单机 TP8（前期报告）

| 模式 | 框架 | c=98 req/s（约） | 说明 |
|------|------|------------------|------|
| 单机 TP8 | vLLM | 105 | 22 上 8 卡合一 |
| 单机 TP8 | SGLang | 87–107 | 视压测端而定 |
| **PD 分离 TP8** | **vLLM** | **122** | 22P + 23D，本报告 |
| 单机 PD | SGLang | 72 | 23 上 1+1 GPU |

PD 分离在 vLLM 上未明显低于单机 TP8，说明 **Prefill/Decode 分置 + KV 跨机** 在该环境中开销可控；SGLang 需环境修复后再测 TP8 跨机。

### 5.4 架构建议

- **生产若需 PD + 双机 TP8**：当前环境优先 **vLLM + P2pNcclConnector + disagg proxy**。  
- **若坚持用 SGLang**：在 22 上对齐 23 的 NIXL/UCX/内核版本后重测；或短期采用 **每机 TP4 + PD**（需验证 SGLang 是否支持同机不同 GPU 组的 TP 切分）。  
- **压测工具**：vLLM PD proxy 走 `/v1/completions`。使用 `sglang.bench_serving` 时须：① 压测前确认 Proxy 响应含 `choices`（§3.1.5.1）；② `--model qwen3-8b` 与 served name 一致；③ `--tokenizer` 指向本地模型目录。Decode 不可用时 Proxy 返回 `{"error":...}` 会导致 `KeyError: 'choices'`（§3.1.5.2）。严格 512/128 用方式 A；历史 §4.1 数据来自方式 B（aiohttp 短 prompt）。

---

## 6. 产物与复现

| 类型 | 路径 |
|------|------|
| vLLM PD 压测结果 | `/root/jianxiong/logs/bench_pd_tp8_vllm_cross_20260517/vllm_pd_summary.json` |
| vLLM Prefill/Decode 容器 | `qwen_pd_vllm_p`（22）、`qwen_pd_vllm_d`（23） |
| vLLM Proxy | `qwen_pd_vllm_proxy`（23） |
| SGLang Decode 日志 | `/root/jianxiong/logs/pd_decode_host.log`（23） |
| 脚本 | `/root/jianxiong/qwen3_8b_pd_tp8_benchmark.sh` |

### 清理

```bash
# 22
docker rm -f qwen_pd_vllm_p qwen_pd_p 2>/dev/null

# 23
docker rm -f qwen_pd_vllm_d qwen_pd_vllm_proxy qwen_pd_bench 2>/dev/null
pkill -f "sglang.launch_server|sglang_router" 2>/dev/null
```

---

**报告结束**
